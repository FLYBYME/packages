/**
 * Global Registry for Action definitions.
 * Developers should augment this interface in their respective modules.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface IServiceActionRegistry {}

/**
 * Global Registry for Event payloads.
 * Developers should augment this interface in their respective modules.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface IServiceEventRegistry {}

/**
 * Global Registry for Service Settings.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface ISettingsRegistry {}
